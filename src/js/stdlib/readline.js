import { c, isColorSupported } from './readline/color.js';
import { createInterface } from './readline/readline.js';

export { createInterface, c, isColorSupported };
export default { createInterface, c, isColorSupported };
